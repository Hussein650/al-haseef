import type { SVGAttributes } from 'react';

export default function AppLogoIcon(props: SVGAttributes<SVGElement>) {
    return (
        <img src="/images/logo.svg" alt="شعار الحصيف" className="w-[60%]" />
    );
}
